# Schemas

Versioned JSON Schemas and compatibility fixtures will live here. Schema filenames include their major version, and breaking changes create a new version instead of silently changing meaning.
