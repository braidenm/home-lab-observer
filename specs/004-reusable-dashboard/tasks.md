# Tasks: Reusable local dashboard

- [x] Accept the focused dashboard package boundary without changing Spec 002.
- [x] Define the transport-neutral view model and `ObserverDataSource`.
- [x] Add a read-only, loopback-constrained local HTTP adapter.
- [x] Add deterministic, network-free synthetic fixtures and demo entry.
- [x] Build responsive Overview, Trends, Workloads, Logs, and Observer Health/Privacy views.
- [x] Exclude process account/owner/user identity, arguments, and environment data.
- [x] Document and test that summaries are code-owned sanitized metadata, never raw message bodies.
- [x] Add strict typecheck, component/adapter/fixture tests, library/demo builds, and bounded CI.
- [ ] Map the accepted Spec 002 API payload into the dashboard view model in a runtime integration specification.
- [ ] Embed compiled assets and add packaged browser accessibility checks in a runtime integration specification.
