# Tasks

This backlog is intentionally high-level until Spec 001 records the architecture decisions.

- [ ] Define observation, capability, quality, retention, and redaction contracts.
- [ ] Implement portable host collectors and OS-specific capability adapters.
- [ ] Implement bounded local history and migration/recovery behavior.
- [ ] Implement the read-only API, health/readiness, and OpenMetrics output.
- [ ] Implement the embedded responsive dashboard and accessibility checks.
- [ ] Implement service, container, and allowlisted log adapters per platform.
- [ ] Implement outbound enrollment, credential lifecycle, policy display, and upload queue.
- [ ] Build service installers and lifecycle helpers for Windows, macOS, and Linux.
- [ ] Build the optional multi-architecture Linux container and constrained Docker proxy profile.
- [ ] Add signed checksums, SBOM, provenance, attestations, and platform canaries.
- [ ] Publish a prerelease, migrate Platform Demo, and document rollback/deprecation.
